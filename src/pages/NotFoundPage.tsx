import Heading from '../components/typography/Heading';
import Layout from '../components/layout/Layout';

const NotFound = () => {
  return (
    <Layout pageTitle="Not Found">
      <Heading title="Error Page Not Found" />
    </Layout>
  );
};

export default NotFound;
